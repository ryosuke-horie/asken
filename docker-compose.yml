version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: asken-postgres
    environment:
      POSTGRES_USER: asken
      POSTGRES_PASSWORD: asken
      POSTGRES_DB: asken
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/database/migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U asken"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
