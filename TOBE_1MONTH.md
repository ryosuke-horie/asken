# 1ヶ月 To-be（検討ログ）

このファイルは、会話で決めた方針を「忘れない」ためのメモです。
更新ルール: 今後のQ&Aで方針が固まるたびに、このファイルへ追記/更新します。

## 前提（As-is / 実装確認済み）

- 現状MVP: 「食事（画像/テキスト）→ Gemini CLIで食材分類/栄養素算出 → 履歴として保存 → 画面表示」
- バックエンド:
  - PostgreSQL 永続化あり（analysis_requests / analysis_results）
  - 非同期分析（POSTで受付 → ワーカーが処理 → GETで結果取得）
- API（主要）:
  - `POST /api/analyze`（画像 or JSONテキスト入力を受付、202でanalysis_id返却）
  - `GET /api/analyze/:id`（ステータス/結果取得）
  - `GET /api/history` / `GET /api/history/:id` / `DELETE /api/history/:id`
  - `GET /api/meals/daily`（日次の食事データ）
  - `GET /api/images/:filename`（保存画像の配信）

## 目的（1ヶ月スコープ）

- ユーザーは現状あなた1人
- 柔術/キックボクシングの減量・体重コントロールを、日々の記録とAI相談で回すツールにする
- 目標設定は「ここで決め打ち」ではなく、アプリ側で補助して合意形成しながら進める

## 日次フロー（起点と合意）

- 日々の起点: 体重入力
- 合意の取り方: 毎日のミニ合意（毎回）
- ミニ合意の起動: 体重入力の直後に自動で開始する（質問が出る）
- ミニ合意の主目的: 「今日の計画」中心（今日の行動を合意して進める）
- 今日の計画で必ず合意する枠:
  - トレーニング（練習する/しない + 場所（複数可）程度。詳細は相談開始時のみ）
  - 食事（厳密な目標管理ではなくアドバイス程度に留める）※記録自体は独立のまま
- 食事アドバイスの扱い:
  - 提案を表示するだけ（合意操作なし）
- 毎日の必須確認項目:
  - 体重・体調・疲労度
  - 摂取/消費カロリーと運動量
- 体重の扱い:
  - 公式体重は「1日1回」
  - 公式体重の計測ルールは「起床後」で固定
  - 体重入力そのものにはAIは関与しない（入力はシンプルに数値を入れるだけ）
  - 減量期などで練習前後に追加計測したくなる想定がある（公式とは別枠で記録できると良い）
  - 追加計測はプラン調整の提案に使う（毎日のミニ合意で反映）

- 疲労度の入力形式:
  - 3段階（低い/普通/高い）

- 体調（コンディション）の入力形式:
  - 3段階（良い/普通/悪い）

- 体調が悪い日の分岐:
  - まず「今日の予定（必須の練習がある等）」を確認してから提案内容を分岐する
  - 予定の入力フロー: 「今日は練習する/しない」→（する場合）「場所（道場/ジム/自宅など、複数可）」を選択
- モード:
  - 「減量期間（本人がそう認識している期間）」などのモードを設定できるようにする
  - モードに応じて、追加計測などの記録をしやすくする
  - モード情報はDBに保存する
  - 管理方法: 期間テーブル（mode + start/end）で管理する
  - モードの種類は固定（通常/減量 の2択）
  - 期間は日付（DATE）で管理（開始日/終了日）
  - 同時にアクティブなモードは1つだけ（期間の重複は禁止）
  - モード期間は開始時に開始日/終了日を両方登録（期間固定）
  - 減量モード期間は終了日を含む（終了日当日までは減量扱い、翌日から通常）
  - 終了日を過ぎたら自動で通常扱いに戻す（確認は出さない）
  - 練習前後の追加計測は「減量モード期間だけ」許可する
  - 追加計測の上限: 1日3回まで（起床後 + 練習前 + 練習後）
  - 練習前/後の区別: 固定ボタン（「練習前」「練習後」）で入力する
  - 追加計測入力後の挙動: 1行フィードバック表示（合意なし）

## 食事管理（独立）

- 食事管理は体重フローとは「独立」させる
- 食事の記録方法:
  - 画像入力 + 文字入力
  - 画像/文字から調査・推定して埋めていく
- 1食の区分:
  - 記録時にユーザーが「朝/昼/夜/間食」を選択して付与する

## 「未記録っぽい」食事の補完と差分レビュー

- 欠損の埋め方: AIが不足箇所を提示し、未記録っぽい分だけ埋める（負担最小）
- 適用は自動ではなく、差分レビューで合意してから反映
- 承認単位: 1食（朝/昼/夜/間食）単位で「承認/修正/却下」
- 逃げ口: 修正指示・却下ができることを前提にする

## 体重入力時に参照する「食習慣」期間

- デフォルト: 直近7日
- AIが期間を自動調整して良いケース（精度が必要なとき）:
  - 予測と実測のズレが続く
  - 入力が曖昧/矛盾している
  - ユーザーが確度を明示要求した
- 精度が必要なとき、AIが最初に追加で取りにいく情報:
  - 食事の詳細（写真/文字・量・時間）

## トレーニングプラン

- 可変要素（プランに反映）:
  - 運動量（回数・強度・種類）
  - 休養・睡眠の優先度
- 基本方針:
  - トレーニングはデフォルトで詳細なメニュー提案をしない
  - トレーニング関連の相談を開始した場合のみ、必要な情報を拾って提案する（疲労度/スケジュール中心）
- トレーニング相談の導線:
  - いったん実装しない（必要になったら追加）
- （必要なら）メニュー立案:
  - その時のタイミング + ユーザーが使用可能な「ツール/場所」を事前設定しておき、そこからメニュー立案
  - 裏側は Gemini CLI を利用する想定
- 事前設定の運用:
  - 初回セットアップで固定し、変更は手動
- 相談開始時の入力:
  - まず「場所」を選ぶ（道場/ジム/自宅など、複数可）
  - 複数の場所がある場合は、場所ごとに別セッションとして扱う（必要なら）
  - （必要なら）その場所で使える器具/環境の事前設定を前提に提案する
- メニュー生成でGeminiに渡す最小入力（必要時）:
  - 今日の場所（複数可）
  - 使える器具リスト
  - 目的（減量/維持）
  - 疲労度
- トレーニング提案の合意（粒度）:
  - 1日の提案をまとめて承認する（必要なら再生成する）
- トレーニング実績の記録（最小）:
  - 実施/未実施だけ
- 未実施日の扱い:
  - 理由は聞かない
  - 計画の自動調整は基本しない（翌日のミニ合意で通常通り計画を作る）
- 「目的（減量/維持）」の決め方:
  - AIが提案し、ミニ合意で確定する
- 減量モード中でも、体重推移 + 疲労度を見て「減量/維持」を柔軟に提案してよい（合意して反映）
- 場所ごとの事前設定:
  - 使える器具/設備のリスト（必要なら重量や本数も）
  - 初期作成は手入力（自由記述）で行い、Geminiで器具名の正規化/同義語吸収をする
  - 正規化結果は正規化名で上書きして保存する（原文は保持しない）
  - 正規化で複数候補になった場合は、候補提示してユーザーが選ぶ（登録前に合意）

## Geminiサポートの範囲（現時点）

- 器具名の特定（写真/自由記述から）

## 画像の保存と削除

- 食事画像は一定期間だけ保存し、自動削除
- 保存期間: 30日
- 30日後:
  - 画像は削除
  - 解析結果（食材・量・栄養素）は残す

## 永続化（保存先）

- 永続化先: PostgreSQL

## 未決（次に決めたい）

- 体重/体調/疲労/トレーニング/合意ログのテーブル設計
- 差分レビューのUI詳細（どこまで編集可能にするか、再解析の扱い）
- 「体調相談」の入力項目とガードレール（安全性・運用）
